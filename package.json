{
  "name": "data-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-assisted business metrics app (MRRpV demo). Queries Databricks; OpenAI interprets intent and invokes query functions.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node backend/src/run.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "node backend/src/run.js",
    "validate:mrrpv-spec": "node scripts/validate-mrrpv-spec.js"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
